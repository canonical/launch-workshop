name: dev
base: ubuntu@24.04
sdks:
  - name: project-node
scripts:
  all: npm run all -- "$@"
  format: npm run format:write -- "$@"
  install: npm ci -- "$@"
  lint: npm run lint -- "$@"
  test: npm run test -- "$@"
